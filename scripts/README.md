# Scripts

Executable scripts for training, evaluation, and feature extraction.

- `train_baseline.py`: Fine-tune VideoMAE on CholecT50 for action recognition.
- `extract_features.py`: Cache spatiotemporal features from VideoMAE for ActionFormer training.
- `train_vqa.py`: Fine-tune the multimodal VQA adapter (Phase 3).
