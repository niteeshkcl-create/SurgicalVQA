# Data Storage

This directory is intended for dataset storage and caching.

- `raw/`: Place raw CholecT50/Cholec80 videos and labels here.
- `processed/`: Preprocessed frames and intermediate labels.
- `cache/`: Cached features (e.g., VideoMAE embeddings) and indices.

**Note**: Large data files in these directories are excluded from git.
